{
  "name": "devdiary",
  "version": "0.1.0",
  "description": "MCP server that auto-generates dev diary entries from git activity",
  "license": "MIT",
  "bin": {
    "devdiary": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "mkdir -p test-output && tsx --test test/test.js test/test.strage.js --test-reporter=tap 2>&1 | tee test-output/results.tap",
    "prepublishOnly": "npm run build",
    "inspect": "npx @modelcontextprotocol/inspector tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
